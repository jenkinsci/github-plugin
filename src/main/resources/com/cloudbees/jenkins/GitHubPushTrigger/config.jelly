<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:nested>
        <table style="width:100%">
            <f:optionalBlock title="${%Override Global Configuration}" inline="true"
                             checked="${instance.isOverrideEventTypes()}" field="overrideEventTypes">

                <f:entry title="${%EventTypes}" field="eventTypes">
                    <f:enumSet/>
                </f:entry>
            </f:optionalBlock>
        </table>
    </f:nested>
</j:jelly>